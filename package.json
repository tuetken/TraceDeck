{
  "name": "tracedeck",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "sdk",
    "sample-data"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint": "^10.0.0",
    "prettier": "^3.8.1"
  }
}
